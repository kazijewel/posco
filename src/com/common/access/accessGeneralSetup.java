package com.common.access;

import com.common.share.SessionBean;
import com.vaadin.ui.Window;

@SuppressWarnings("serial")
public class accessGeneralSetup extends Window
{
	public accessGeneralSetup(SessionBean sessionBean)
	{
		this.setVisible(false);
	}
}